<%= render 'shared/header' %>

<%= form_with model: @task, local: true do |f| %>
  <div id="tasks-container">
    <div class="task-fields">
      <%= f.label :content, "タスク" %><br />
      <%= f.text_field :content %>
    </div>
  </div>

  <%= submit_tag '送信' %>
<% end %>
