<%= render 'shared/header' %>
  
<%= month_calendar do |date| %>
  <%= link_to date.day, calendar_day_path(date), class: "day-link" %>
<% end %>

<% if user_signed_in? %>
  <%= link_to '今日の気分は？', new_mood_path, class: 'floating-mood-link' %>
<% end %>
